# Single-machine dev profile (in-process scorer allowed).
# Used by: python infra/deploy.py --env dev --role all

TRADING_MODE=paper
IBKR_ACCOUNT_TYPE=paper
IBKR_PORT=7497
PAPER_MODE=1
PROFIT_PROFILE=balanced

TRADER_IN_PROCESS_SCORER=auto
DOCKER_HEALTHCHECK_REQUIRE_RESEARCH=0
DOCKER_HEALTHCHECK_MDA=0
