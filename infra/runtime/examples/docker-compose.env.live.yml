# Live IBKR/TWS profile override (use with deploy.py --env live).
services:
  abc-trader:
    environment:
      TRADING_MODE: live
      IBKR_ACCOUNT_TYPE: live
      IBKR_PORT: "7496"
      PAPER_MODE: "0"
      PROFIT_PROFILE: ${PROFIT_PROFILE:-balanced}
