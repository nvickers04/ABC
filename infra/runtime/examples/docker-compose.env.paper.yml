# Paper IBKR/TWS profile override (use with deploy.py --env paper).
services:
  abc-trader:
    environment:
      TRADING_MODE: paper
      IBKR_ACCOUNT_TYPE: paper
      IBKR_PORT: "7497"
      PAPER_MODE: "1"
      PROFIT_PROFILE: ${PROFIT_PROFILE:-balanced}
